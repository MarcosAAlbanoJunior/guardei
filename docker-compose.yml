services:
  guardei:
    build: .
    image: guardei:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - guardei-data:/data
    mem_limit: 512m # o alvo é rodar em uma VPS de 512 MB

volumes:
  guardei-data:
